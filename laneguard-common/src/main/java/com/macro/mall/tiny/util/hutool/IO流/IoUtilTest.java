package com.macro.mall.tiny.util.hutool.IO流;

import org.junit.Test;

/**
 * @ClassName IoUtilTest
 * @Author WB-918966
 * @Date 2022/10/21 16:13
 * @Version 1.0
 **/
public class IoUtilTest {
    @Test
    public void test(){
        //从流中读取内容，读取完毕后关闭流
        //IoUtil.read(inputStream,charset);
        //从Reader中读取集合
        //Collection<String> strings = IoUtil.readLines(reader, list);
        //从流r中读取字节
        //byte[] bytes = IoUtil.readBytes(inputStream);
        //将byte[]写到流中
        //IoUtil.write(outputStream,isCloseOut,content);
        //将Reader中的内容复制到Writer中 使用默认缓存大小，拷贝后不关闭Reader
        //IoUtil.copy(reader,writer);
        //从缓存中刷出数据
        //IoUtil.flush(flushable);
        //IoUtil.getReader(inputStream,charset);
        //IoUtil.getWriter(outputStream,charset);
        //IoUtil.toStream(file);
        //IoUtil.toBuffered(inputStream);
        //IoUtil.contentEquals(input1,input2);
        //IoUtil.lineIter(reader);
        //IoUtil.toStr(out,charset);
    }
}
